# Feature modules for Omega Ice, split out of app.py as Flask Blueprints
# so each one can be developed/debugged on its own instead of everything
# living in one giant file. See modules/shared.py for the common helpers
# every module uses (Firebase read/write, login guards).
